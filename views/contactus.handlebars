<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>The Foliage Fix</title>

  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://fonts.googleapis.com/css?family=Work+Sans:200,300,400,500,600,700" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Pacifico|Montserrat|Roboto" rel="stylesheet">
  <style type="text/css">
    
    .bg-white {
      background: white;
    }

    .banner {
      width: 100%;
      height: auto;
      padding-top: 20px;
      padding-bottom: 20px;
      align-content: center;
      align-items: center;
      text-align: center;
    }

    .banner-logo {
      width: 150px;
      height: auto;
      margin: 0 auto;
    }

    .center {
      text-align: center;
      align-content: center;
      align-items: center;
    }

    .border {
      border: 1px solid darkgray;
    }

    .intro {
      font-size: 1.25rem;
      font-weight: 200;
      letter-spacing: 0.05rem;
      padding-top: 20px;
      padding-bottom: 20px;
    }

    body {
      font-family: 'Montserrat', sans-serif;
      font-size: 0.8rem;
    }

    table {
      width: auto;
      max-width: 600px;
      margin: 0 auto;
      padding: 10px;
    }

    .label {
      width: 25%;
      padding: 10px 10px;
    }

    .replyButton {
      background-color: #6c9f7f;
      width: fit-content;
      color: white !important;
      cursor: pointer;
      display: block;
      padding: .5rem 1rem;
      border-color: #6c9f7f;
      font-weight: 400;
      text-align: center;
      vertical-align: middle;
      font-size: 1.1rem;
      border-radius: .25rem;
      text-decoration: none;
      margin: 30px auto;
    }

  </style>
</head>
<body>

  <div>
    <div class="banner center">
      <div class="banner-logo center">
        <img class ="banner-logo" src="https://indoor-plants-nz-assets.s3.ap-southeast-2.amazonaws.com/logo_transparent_background.png"/>
      </div>
    </div>

    <div class="center intro">
      <span>Hi Carlos, you have {{ type }}!</span>
    </div>

    <div>
      <table class="table border bg-white">
        <tbody>
          <tr>
            <td class="label">Name</td>
            <td>{{ name }}</td>
          </tr>
          <tr>
            <td class="label">Contact</td>
            <td>{{ contact }}</td>
          </tr>
          <tr>
            <td class="label">Email</td>
            <td>{{ email }}</td>
          </tr>
          <tr>
            <td class="label">Message</td>
            <td>{{ message }}</td>
          </tr>
        </tbody>
      </table>
    </div>

    <div>
      <a href="mailto:{{ email }}" class="replyButton">Send a reply</a>
    </div>

  </div>
</body>

</html>
